/* --------------------
 * @overlook/router-path module
 * Tests
 * ------------------*/

'use strict';

// Modules
const pathRouterExtend = require('../index');

// Init
require('./utils');

// Tests

describe('tests', () => {
	it.skip('all', () => { // eslint-disable-line jest/no-disabled-tests
		expect(pathRouterExtend).not.toBeUndefined();
	});
});
